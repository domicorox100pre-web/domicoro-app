name: Deploy to DigitalOcean

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Build Frontend
        run: |
          cd frontend
          npm install        # ← CAMBIADO: npm install en vez de npm ci
          npm run build
      
      - name: Deploy to DO Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          script: |
            cd /var/www/domicoro
            git pull origin main
            cd backend && npm install --production   # ← CAMBIADO AQUÍ TAMBIÉN
            sudo systemctl restart domicoro || sudo docker-compose restart
